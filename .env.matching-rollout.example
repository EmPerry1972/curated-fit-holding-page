# Phase A client matching rollout. Server-side values only.
# Do not expose these through NEXT_PUBLIC_* variables.
MATCHING_CLIENT_ROLLOUT_ENABLED=false
MATCHING_ROLLOUT_DRY_RUN_ONLY=true
AIRTABLE_MATCHING_ROLLOUT_BASE_ID=appgYLxrpdZXXULDf
AIRTABLE_MATCHING_ROLLOUT_TOKEN=

# Existing internal client-test access remains required.
MATCHING_CLIENT_TEST_SECRET=
